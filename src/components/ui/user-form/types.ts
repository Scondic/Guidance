type FormDataType = {
  firstName: string,
  lastName: string,
  birthDate: string,
  sex: typeof Option,
  city: string,
  // meeting: [] as Option[],
  // role: [] as Option[],
};

export type { FormDataType }
